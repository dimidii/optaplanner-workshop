package domain;

public class TimeTable {
    // TODO Create the TimeTable object
    /**
     * This class should have the following variables: timeslotList(List<Timeslot>), roomList(List<Room>), lessonList(List<Lesson>), score(HardSoftScore), solverStatus(SolverStatus)
     * Tip: make all the variables private and provide getters. Provide setter for solverStatus variable.
     * Multiple constructors: one empty, one with all variables
     */
}
